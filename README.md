Open your WSL Terminal (e.g., Ubuntu).
Navigate to the project folder by translating your Windows path to a WSL path
Run:

  `cd /mnt/c/Users/Lenovo/Desktop/inventory-management-system-main `

  Then to Run the docker containers;

  `docker compose up --build `





 
